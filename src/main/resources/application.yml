spring:
  datasource:
    url: jdbc:mysql://localhost:3306/snack
    username: root
    password: sook610017!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true